/*
Write a function that will accept two numbers and return true if the first number can be divided without any remainder (remainder should be zero), 
otherwise should return false.
*/

function sfs(number1, number2) {
    if (number1 % 2 !== 0)
}