const task = require('./task-7');

test('result of the expression', () => {
  expect(task.resultOfExpression1()).toBe(false);
});

test('result of the expression', () => {
    expect(task.resultOfExpression2()).toBe(true);
  });

  test('result of the expression', () => {
    expect(task.resultOfExpression3()).toBe(true);
  });

  test('result of the expression', () => {
    expect(task.resultOfExpression4()).toBe(false);
  });