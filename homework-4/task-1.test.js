const task = require('./task-1');

test('results of these boolean expressions', () => {
  expect(task.booleanValue()).toBe(true);
});

test('results of these boolean expressions', () => {
    expect(task.booleanValue2()).toBe(false);
  });

  test('results of these boolean expressions', () => {
    expect(task.booleanValue3()).toBe(false);
  });

  test('results of these boolean expressions', () => {
    expect(task.booleanValue4()).toBe(false);
  });

  test('results of these boolean expressions', () => {
    expect(task.booleanValue5()).toBe(true);
  });

  test('results of these boolean expressions', () => {
    expect(task.booleanValue6()).toBe(true);
  });

  test('results of these boolean expressions', () => {
    expect(task.booleanValue7()).toBe(true);
  });

  test('results of these boolean expressions', () => {
    expect(task.booleanValue8()).toBe(false);
  });