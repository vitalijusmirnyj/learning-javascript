/*
What would be the result of the expression below and why?
(10 > 0) && (10 < 50)
*/

function resultOfExpression() {
    return (10 > 0) && (10 < 50);
}

module.exports.resultOfExpression = resultOfExpression;