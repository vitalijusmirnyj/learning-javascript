/*
Task: Create a function that can multiply two big numbers (e.g. trillion times trillion).
Hint: BigInt data type could help here.
*/

function twoBigNumbersMultiplication(number1, number2){
    return number1 * number2;
}

module.exports.twoBigNumbersMultiplication = twoBigNumbersMultiplication;