/*
Task; Create a function that computes the log10 of an absolute number. 
Hint: check if you can reuse an already existing function for this.
*/

function findLog10(number) {
    let result = Math.log10(number);
    return result;
}

module.exports.findLog10 = findLog10;

