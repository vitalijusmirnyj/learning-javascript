/*Write a function that will accept two parameters (text, index) and will return a single letter at the index but changed to the uppercase.
 E.g. yourFunction(‘Hello, 1) should return ‘E’.
*/

function characterAtIndexToUpperCase(text, index){ 
    return text.charAt().toUpperCase();
}

module.exports.characterAtIndexToUpperCase = characterAtIndexToUpperCase;