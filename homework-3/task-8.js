//Write a function that will accept two parameters (text, searchText) and will return true if text contains searchText.

function ifIncludes(text, searchText) {
    return text.includes(searchText);
}

module.exports.ifIncludes = ifIncludes;