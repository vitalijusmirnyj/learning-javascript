/*
Task: Write a function that will accept a single parameter (text) and will return the same string but with all letters changed to lowercase.
*/

function textToLowerCase(string) {
    return string.toLowerCase();
}

module.exports.textToLowerCase = textToLowerCase;
