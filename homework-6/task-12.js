/*Implement all of the functions that you created above but now instead of using for loop now use while..do instead.
1.Write a function that will accept an array of numbers and will output (using console.log) each odd number starting from the first one to the last one.
*/

function eachOddNumber(array) {
    while (i < array.lenght) {
        i = 0;
        i++;
    }
        if (array[i] % 2 !== 0) {
            console.log(array[i]);
        }
    }

    module.exports.eachOddNumber = eachOddNumber;

   

