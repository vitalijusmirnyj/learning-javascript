//Write a function that will accept an array of numbers and will output (using console.log) each number from the first one to the last one.

function arrayOfNumbers(array) {
    for (let i = 0; i < array.length; i++) {
        console.log(array[i]);
    }
}